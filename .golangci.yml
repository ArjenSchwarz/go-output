version: "2"

run:
  timeout: 5m
  tests: false

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - misspell
    - staticcheck
    - unused
    - revive
    - goconst
    - gocritic
    - unconvert
  settings:
    revive:
      rules:
        - name: exported
          disabled: false
          arguments:
            - "disableStutteringCheck"

issues:
  max-issues-per-linter: 0
  max-same-issues: 0